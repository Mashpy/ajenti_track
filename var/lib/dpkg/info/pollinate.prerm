#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/pollinate" ] || [ -e "/etc/init/pollinate.conf" ]; then
	invoke-rc.d pollinate stop || exit $?
fi
# End automatically added section
